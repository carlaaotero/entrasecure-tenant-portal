<!DOCTYPE html>
<html lang="ca">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title><%= title %> · EntraSecure</title>
  <!-- Enllaça el CSS servit des de /public/css/styles.css -->
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <!-- Navbar senzilla -->
  <nav class="navbar">
    <div class="brand">EntraSecure Tenant Portal</div>
    <ul>
      <li><a href="/">Home</a></li>
      <!-- placeholders per a futurs mòduls -->
      <li><span class="muted">My Identity</span></li>
      <li><span class="muted">Tenant Explorer</span></li>
      <li><span class="muted">Security</span></li>
    </ul>
    <div class="auth">
      <% if (user) { %>
        <!-- Si hi ha sessió, mostrem el nom i un logout -->
        <span>Hola, <%= user.name || user.username || 'usuari' %></span>
        <a href="/auth/logout">Logout</a>
      <% } else { %>
        <!-- Si no hi ha sessió, oferim login -->
        <a class="btn" href="/auth/login">Login (MSAL)</a>
      <% } %>
    </div>
  </nav>

  <main class="container">
    <h1>Benvingut/da 👋</h1>
    <p>Base del TFG: Express + EJS + sessions. Preparat per integrar MSAL/Graph.</p>

    <div class="card">
      <h2>Estat</h2>
      <ul>
        <li>Express configurat amb EJS</li>
        <li>Sessions preparades</li>
        <li>Rutes: <code>/</code>, <code>/auth/login</code>, <code>/auth/redirect</code>, <code>/auth/logout</code></li>
      </ul>
    </div>
  </main>

  <footer class="footer">
    <small>&copy; <%= new Date().getFullYear() %> EntraSecure · TFG</small>
  </footer>
</body>
</html>
